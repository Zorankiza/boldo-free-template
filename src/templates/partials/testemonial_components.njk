<div class="mx-5 bg-white border rounded-lg max-w-xs py-10 px-10 mb-10 md:mb-0">
    <p class="text-2xl mb-10">{{ cards.text }}</p>
    <div class="flex">
        <img src="{{ cards.imgUrl }}">
        <div class="ml-4">
            <h3 class="font-bold">{{ cards.name }}</h3>
            <p>{{ cards.link }}</p>
        </div>
    </div>
</div>