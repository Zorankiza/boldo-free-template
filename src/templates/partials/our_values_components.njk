<div class="flex md:flex-row flex-col justify-between my-9">
    <img class="" src="{{ card.imgUrl }}">
    <div class="flex flex-col max-w-xl">
        <h3 class="text-white mb-4 text-3xl">{{ card.title }}</h3>
        <p class="text-gray-200 text-xl">{{ card.text }}</p>
    </div>
</div>