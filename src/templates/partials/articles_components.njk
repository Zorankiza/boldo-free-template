<div class="flex flex-col max-w-xs md:mb-20 mb-10 ">
    <div class="mb-6">
        <img src="{{ cards.imgUrl }}">
    </div>
    <div>
        <div class="flex mb-3">
            <p class="text-darkBlue font-bold text-base mr-3">{{ cards.category }}</p>
            <p class="text-base text-gray-700">{{ date }}</p>
        </div>
        <div>
            <p class="text-xl mb-5" >{{ cards.text }}</p>
        </div>
        <div class="flex items-center mt-auto ">
            <img class="mr-3" src="{{ cards.avatarImgUrl}} ">
            <p>{{cards.name}}</p>
        </div>
    </div>
</div>